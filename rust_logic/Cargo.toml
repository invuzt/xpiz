[package]
name = "rust_logic"
version = "0.1.0"
edition = "2021"

[lib]
name = "hello" # INI HARUS 'hello' biar jadi libhello.so
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"

# Trigger Build Baru
