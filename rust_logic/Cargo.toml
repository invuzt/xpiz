[package]
name = "xpiz_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "xpiz_core"
crate-type = ["staticlib", "cdylib"]

[dependencies]
jni = "0.21.1"
# Kita ganti fiturnya agar sesuai dengan versi 0.7.0
ndk = { version = "0.7.0", features = ["media", "jni"] }
ndk-sys = "0.4.0"
