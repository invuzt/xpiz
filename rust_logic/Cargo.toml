[package]
name = "xpiz_engine"
version = "1.0.0"
edition = "2021"

[lib]
name = "xpiz_engine"
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1"

[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
panic = 'abort'
strip = true
