[package]
name = "xpiz_core"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib", "cdylib"]

[dependencies]
jni = "0.21.1"
# Tambahkan library AI Smartcore
smartcore = { version = "0.3.0", default-features = false }
