[package]
name = "rust_logic"
version = "0.1.0"
edition = "2021"

[lib]
name = "hello"
crate-type = ["cdylib", "staticlib"]

[dependencies]
jni = "0.21"
zip = { version = "0.6", default-features = false }
