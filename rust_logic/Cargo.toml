[package]
name = "rust_logic"
version = "0.1.0"
edition = "2021"

[lib]
name = "hello"
crate-type = ["cdylib", "staticlib"]

[dependencies]
jni = "0.21"
# Library standar untuk manajemen data
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
