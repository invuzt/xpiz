[package]
name = "xpiz_core"
version = "0.1.2"
edition = "2021"

[lib]
name = "xpiz_core"
crate-type = ["cdylib"] # Sangat penting untuk Direct JNI tanpa C

[dependencies]
jni = "0.21.1"
evalexpr = "8.1"

[profile.release]
opt-level = 'z'
lto = true
codegen-units = 1
panic = 'abort'
strip = true
